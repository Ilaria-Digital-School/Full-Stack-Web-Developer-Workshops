# Find the minimum in a list

THEORETICAL RECAP Finding the minimum in a list is a fundamental algorithmic pattern you will reuse constantly. The idea is simple: you traverse a list of values once, keeping track of the smallest value seen so far. The correct mental model is that of a running champion: you assume the first element is the smallest (the current champion), then you compare it with every following element. Whenever you find a smaller value, it becomes the new champion. When the traversal ends, the champion holds the minimum. Key definitions: a list is an ordered collection of values (see your previous chapter on lists); traversal means visiting each element one by one, typically with a loop; the accumulator variable (here, the current minimum) stores intermediate results across iterations. Common mistakes to avoid: initializing the minimum to 0 or to a fixed number instead of the first element of the list (this breaks when all values are positive or all negative); comparing with the wrong operator (using > instead of

[View the full workshop](https://www.ilaria-academy.com/nextjob/full-stack-web-developer/en/a83c67c7-88c8-45e4-afa1-a27abbc69b14/d2729b04-1729-49ba-bf0d-8e643754054f/find-the-minimum-in-a-list)
