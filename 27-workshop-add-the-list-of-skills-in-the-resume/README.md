# Workshop: add the list of skills in the resume

**Theoretical Recap** HTML provides three types of lists, each with a distinct semantic purpose. The unordered list (``) is used when the order of items does not matter — a perfect fit for a set of technical skills. The ordered list (``) is reserved for sequences where order is meaningful, such as steps in a process. The definition list (``) pairs terms (``) with their descriptions (``), useful for glossaries or labeled data. Every list item in `` and `` uses the `` tag. A common beginner mistake is using `` tags or plain paragraphs to simulate a list. This breaks semantic meaning: screen readers, search engines, and other tools rely on proper list markup to understand the structure of your content. Another frequent error is nesting lists incorrectly — a nested `` must always sit inside an ``, not directly inside the parent ``. Mental model: think of a list as a container (`` or ``) that holds items (``). You can group skills by category using nested lists, but keep nesting to one or two levels maximum for readability. Mini example: ``` HTML5 CSS3 JavaScript ``` This simple structure is clean, semantic, and accessible. Now it is time to apply this directly to the resume project you started in the mini-project activity. --- **Workshop Brief** In this workshop, you will extend the semantic HTML resume you built in the mini-project "creating your semantic resume" by adding a well-structured, semantically correct section dedicated to your technical and personal skills....

[View the full workshop](https://www.ilaria-academy.com/nextjob/full-stack-web-developer/en/27ff0726-3de4-4616-b0be-d9a166415d04/ad0a181a-5ae6-4455-9f7e-9b0a5fd7b726/workshop-add-the-list-of-skills-in-the-resume)
