# Workshop: replacing divs with semantic tags

**Theoretical Recap** Before diving into the practical work, here is a concise recap of what you need to keep in mind. HTML5 introduced semantic tags — elements that carry meaning about the content they wrap, rather than just grouping content visually. Instead of stacking generic `` containers everywhere, semantic HTML tells both the browser and any reader (human or machine) what role each part of the page plays. The key tags you must know: `` wraps the introductory content or navigation at the top of a page or section; `` contains navigation links; `` holds the primary, unique content of the page (used only once per page); `` groups thematically related content; `` represents self-contained content that could stand alone (a blog post, a news item); `` contains content tangentially related to the main content (a sidebar, a callout box); `` wraps closing content — credits, links, copyright. Common mistakes to avoid: nesting `` more than once, using `` as a generic wrapper when a `` is actually more appropriate, placing `` outside of `` when it belongs inside, or using `` for content that is not truly self-contained. Mental model: think of a newspaper. The banner and logo = ``. The menu bar = ``. The central story = `` + ``. The column notes = ``. The credits at the bottom = ``. Mini example: ``` ... ... ... ... ``` This structure replaces a tangle of unnamed `` blocks and makes your code immediately readable and maintainable....

[View the full workshop](https://www.ilaria-academy.com/nextjob/full-stack-web-developer/en/27ff0726-3de4-4616-b0be-d9a166415d04/c6e33bdd-cc42-4214-9f81-b6b799c2b954/workshop-replacing-divs-with-semantic-tags)
