# Workshop: Use time, code and abbr correctly

**Theoretical Recap** Three HTML inline tags are frequently misused or overlooked: ``, ``, and ``. Each carries a precise semantic meaning that goes beyond mere visual formatting. The `` tag marks up a human-readable date, time, or duration and pairs it with a machine-readable `datetime` attribute. For example, `March 15, 2024` tells both search engines and assistive technologies exactly what moment is being referenced. A common mistake is omitting the `datetime` attribute, which strips away the machine-readable value entirely. The `` tag identifies a fragment of computer code — a variable name, a function call, a file path, a command — within a sentence. It does not replace a full code block (for that, wrap `` inside ``), but it is essential for semantic clarity inside prose. Do not use `` simply to display text in a monospace font; use it only when the content is genuinely code. The `` tag marks an abbreviation or acronym and takes a `title` attribute containing the full expanded form: `HTML`. On hover, browsers display the expansion. The most common mistake is using `` without a `title`, which defeats its entire purpose. Mental model: ask yourself — is this content a point in time or duration? Use ``. Is it a piece of code or a technical token? Use ``. Is it a shortened form of a longer term? Use ``. Never use these tags for styling alone. --- **Workshop Brief** In this workshop you will build a single, self-contained HTML page that demonstrates...

[View the full workshop](https://www.ilaria-academy.com/nextjob/full-stack-web-developer/en/27ff0726-3de4-4616-b0be-d9a166415d04/98850571-ba5b-4006-aa92-e08c97e6d85c/workshop-use-time-code-and-abbr-correctly)
