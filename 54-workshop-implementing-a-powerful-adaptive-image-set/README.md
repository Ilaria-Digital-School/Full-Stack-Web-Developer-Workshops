# Workshop: implementing a powerful adaptive image set

**Theoretical Recap** Adaptive images are not simply about making images look good on all screens — they are about delivering the right image, at the right resolution, to the right device, at the right time. The two core HTML attributes you need to master are `srcset` and `sizes`. The `srcset` attribute lets you declare multiple image sources at different resolutions or widths (using `w` descriptors or `x` descriptors), while `sizes` tells the browser how wide the image will actually be rendered at various viewport widths, so it can make an intelligent choice before downloading anything. Lazy loading, achieved via the `loading='lazy'` attribute, defers the loading of off-screen images until the user scrolls near them, reducing initial page load time significantly. The `` element goes further, allowing art direction: you can serve entirely different image crops or formats (WebP, AVIF, JPEG) depending on the device context using `` elements with `media` and `type` attributes. A common mistake is confusing `srcset` with CSS `background-image` — `srcset` only works on `` and `` elements, not inline styles. Another frequent error is omitting the `sizes` attribute when using `w` descriptors, which causes browsers to fall back to default behavior and often download unnecessarily large images. Mental model: think of `srcset` as the menu of options you offer, `sizes` as the order the browser places, and `` as the chef who can serve a completely different dish depending on the context. **Workshop Brief** This workshop directly follows the lesson on performance optimization with responsive images...

[View the full workshop](https://www.ilaria-academy.com/nextjob/full-stack-web-developer/en/b5c8d528-073f-4bf5-a2fe-f33bf0e6b412/916ed178-4510-48e7-aa2d-6d890f4185a3/workshop-implementing-a-powerful-adaptive-image-set)
